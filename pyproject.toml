[project]
name = "differential-privacy-demo"
version = "0.1.0"
description = "Educational Laplace and Gaussian differential privacy mechanisms from scratch."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
